{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch MyApp",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/Debug/OpenNavSim",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb", // or "lldb" or "gdb" depending on your OS
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}